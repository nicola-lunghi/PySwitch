services:
  python:
    build: .
    container_name: pyswitch-test
    command: sleep infinity
    #ports:
      #- "80:80"
    volumes:
      - ../content/lib:/project/lib
      - .:/project/test
